# Audit Assignment System (WIP)

Streamlit-based audit assignment tool with RBAC (Admin/Auditor), audit creation, skill matching, and timetable scheduling.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
